# راهنمای عامل هوش مصنوعی — پروژه هفت منظومه

این فایل قرارداد اصلی کار روی این مخزن است. **قبل از هر تغییری آن را بخوان.**

## زبان

- با کاربر **فارسی** حرف بزن.
- کامنت‌های کد فارسی، نام‌های کد انگلیسی.
- متن رابط کاربری و پیام خطای کاربر فارسی.
- پیام لاگ سرور انگلیسی.

## پروژه در یک پاراگراف

هفت منظومه یک طلافروشی آنلاین تخصصی کودکان است که زیر پوسته آن یک پلتفرم «گنجینه طلای کودک»
ساخته می‌شود: والدین و اطرافیان کودک به‌جای هدایای مصرفی طلا می‌خرند و به مرور یک دارایی
واقعی برای کودک می‌سازند. هدیه‌دهندگان با یک لینک و بدون ساخت حساب مشارکت می‌کنند.

## سه گام اجباری قبل از شروع هر تسک

۱. `docs/CONTEXT.md` را بخوان — وضعیت فعلی پروژه.
۲. `docs/01-architecture/module-registry.md` را بخوان — **مطمئن شو چیزی که می‌خواهی بسازی
   قبلاً وجود ندارد.** این مهم‌ترین محافظ در برابر دوباره‌کاری است.
۳. `docs/00-overview/glossary.md` را بخوان — نام‌گذاری‌ها را دقیقاً از آنجا بردار.

اگر تسک به دامنه مالی مربوط است، `docs/02-domain/domain-rules.md` را هم بخوان.

## سه گام اجباری در پایان هر تسک

۱. `npm run verify` را اجرا کن و تا سبز شدن ادامه بده.
۲. مستندات مربوطه را به‌روز کن (جدول «چه زمانی چه چیزی» در `docs/README.md`).
۳. `docs/CONTEXT.md` را به‌روز کن. **بدون استثنا.**

## قوانین معماری

چهار لایه با جریان وابستگی یک‌طرفه:

```
app/  →  modules/  →  server/  →  shared/
```

- `app/` نازک است: فقط مسیریابی و چیدمان. هیچ منطق کسب‌وکاری آنجا نمی‌نویسیم.
- `modules/x` از طریق `index.ts` برای صفحات سرور قابل دسترسی است. فایل `"use client"`
  و لایه `domain/` از `ui/`، `actions/` یا `domain/` ماژول دیگر import می‌کنند تا
  `server-only` وارد باندل مرورگر نشود.
- `modules/x/domain/` خالص است: بدون دیتابیس، بدون React، بدون Next.
- `shared/` هیچ دانشی از دامنه ندارد. کامپوننتی که بداند «گنجینه» چیست جای آن `modules/` است.
- دسترسی به دیتابیس فقط از `repo/`.

جزئیات کامل: `docs/01-architecture/overview.md`

## قوانین غیرقابل مذاکره دامنه

اگر یکی از این‌ها را نقض کنی، باگ مالی تولید کرده‌ای:

۱. **هیچ عدد اعشاری در محاسبات مالی.** پول = ریال صحیح، طلا = میلی‌گرم صحیح،
   درصد = صدم درصد صحیح. تقسیم فقط با `mulDiv`. تبدیل واحد فقط در نمایش.
۲. **دفتر کل طلا append-only است.** هیچ `UPDATE` یا `DELETE` روی `gold_ledger_entries`.
   موجودی هرگز ذخیره نمی‌شود، همیشه محاسبه می‌شود. اصلاح = قلم جدید با `source='correction'`.
۳. **کودک، دارنده حساب و مالک دارایی سه چیز جدا هستند.** هرگز یکی نگیرشان.
۴. **قیمت هنگام ثبت سفارش قفل می‌شود** و کل ریزمحاسبات ذخیره می‌گردد. سفارش قدیمی
   هرگز بازمحاسبه نمی‌شود.
۵. **طلا فقط پس از تایید قطعی پرداخت وارد دفتر کل می‌شود.**
۶. **زمان‌ها همیشه epoch میلی‌ثانیه UTC** ذخیره می‌شوند؛ تبدیل به جلالی فقط در نمایش.

## قوانین رابط کاربری

- **هیچ رنگ hard-coded.** فقط توکن‌ها: `bg-card`، `text-muted-foreground`، `bg-gold`.
  نه `#fff`، نه `text-gray-500`، نه `bg-yellow-400`.
- **هیچ کلاس جهت‌دار فیزیکی.** `ms-2` نه `ml-2`؛ `text-start` نه `text-left`؛
  `ps-4` نه `pl-4`؛ `start-0` نه `left-0`.
- اعداد قابل‌مشاهده کاربر با ارقام فارسی (`toPersianDigits`).
- ورودی عددی کاربر با `toEnglishDigits` نرمال شود (کاربر ممکن است ارقام فارسی تایپ کند).
- پیش‌فرض Server Component است؛ `"use client"` فقط وقتی لازم است و تا جای ممکن پایین درخت.
- هر ورودی فرم از `FormField` استفاده کند تا برچسب و خطا یکدست باشد.

## قوانین امنیتی

- بررسی دسترسی **هم در صفحه و هم در Server Action**. بررسی فقط در لایوت کافی نیست،
  چون Server Action یک endpoint قابل فراخوانی مستقیم است.
- هر ورودی بیرونی با Zod اعتبارسنجی شود، با پیام خطای فارسی.
- کد یک‌بارمصرف و توکن سشن فقط هش‌شده ذخیره می‌شوند.
- فایل‌های آپلودی بیرون از `public/` و با سرو کنترل‌شده.
- کلاینت هرگز مبلغ نهایی را تعیین نمی‌کند.
- صفحه عمومی هدیه فقط نام کوچک کودک، سن و پیشرفت را نشان می‌دهد؛ هیچ داده تماس یا شناسه داخلی.

## دیتابیس

- تغییر اسکیما ← `npm run db:generate` ← کامیت فایل مایگریشن.
- **فایل مایگریشن قدیمی هرگز ویرایش نمی‌شود.** اصلاح = مایگریشن جدید.
- `db:push` فقط در توسعه محلی، هرگز در production.
- عملیات چندجدولی حتماً در تراکنش.

## چیزهایی که عمداً نمی‌سازیم

مارکت‌پلیس، Gold Cashback، شبکه شرکا، اپ نیتیو، گیمیفیکیشن پیچیده، فروش و بازخرید طلا،
خرید دوره‌ای خودکار، پیام ویدیویی.

دلیل هرکدام در `docs/06-decisions/0009-out-of-scope-mvp.md`. **این‌ها را به پیشنهاد خودت اضافه نکن.**

## سبک کار

- کوچک و کامل کار کن: یک ماژول را تا آخر ببر، نه ده ماژول نیمه‌کاره.
- اگر برای تسکی نیاز به شکستن یکی از قوانین بالا داشتی، **اول بپرس**؛ خودسرانه نقض نکن.
- اگر تصمیم معماری جدیدی گرفتی، یک ADR در `docs/06-decisions/` بنویس.
- کامنت فقط برای چیزی که کد نمی‌تواند بگوید. کامنت توضیح‌دهنده تغییر خودت ننویس.

## دستورها

```bash
npm run dev          # محیط توسعه
npm run verify       # تایپ‌چک + لینت + بررسی ساختار + تست
npm run db:generate  # ساخت مایگریشن
npm run db:migrate   # اعمال مایگریشن
npm run db:seed      # داده اولیه
npm run db:studio    # مرورگر دیتابیس
npm run check:arch   # بررسی سلامت معماری و مستندات
```
